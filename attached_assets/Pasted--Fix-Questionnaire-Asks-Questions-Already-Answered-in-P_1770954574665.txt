# Fix: Questionnaire Asks Questions Already Answered in Profile

## THE PROBLEM

When a user clicks a form to generate a filled PDF, the system shows an “Additional Information Needed” modal asking questions. Many of these questions are for fields that the user’s profile ALREADY has data for. For example:

- Profile has “Menu Items: Platters (Main Platter, P…)” at 100% confidence → but questionnaire asks “List all food and beverage items to be prepared and served”
- Profile has detailed food transport info → but questionnaire asks “Describe how frozen, cold, and hot foods will be transported”
- Profile has water supply, sanitizing method, waste water info → but questionnaire might ask about these too

The questionnaire should ONLY ask about fields that genuinely can’t be filled from existing profile/vault data.

## PROMPT FOR REPLIT

```
The "Additional Information Needed" questionnaire is asking users questions that their profile data ALREADY answers. This is a bad user experience — the user uploaded documents, Gemini parsed them at 95-100% confidence, but the questionnaire ignores all that parsed data.

**THE ROOT CAUSE:**

Find the `analyze-questions` endpoint: `POST /api/towns/:townId/forms/:formId/analyze-questions` in `server/routes.ts`. This endpoint:
1. Reads the PDF form fields
2. Compares them against the user's data (vault or profile)
3. Returns a list of "unanswered" fields as questions

The problem is in step 2 — the matching between PDF field names and user data keys is too strict. For example:
- PDF field: "1. List all food and beverage items to be prepared and served (Line 1)"
- User data key: "menu_items" with value "Platters (Main Platter, Picanha, etc.)"
- These SHOULD match, but the matcher doesn't recognize them as the same thing

**FIX PART 1 — Improve the field-to-data matching in analyze-questions:**

The analyze-questions endpoint needs to use the SAME smart matching logic as the actual PDF filling. Look at `smartMatchFieldToData()` in `pdf-service.ts` (the big if/else chain around lines 691-873). The analyze-questions endpoint should use this same function to determine which fields CAN be filled.

Specifically:
- For each PDF form field, run it through smartMatchFieldToData() with the user's full data map
- If smartMatchFieldToData returns a match → this field is ANSWERED, don't ask about it
- If smartMatchFieldToData returns no match → this field is genuinely missing, add it to questions list

**FIX PART 2 — Build a COMPLETE data map before comparing:**

The user's data comes from multiple sources. Before checking which fields are missing, build a comprehensive data map that includes EVERYTHING:

```typescript
function buildCompleteDataMap(profile: any, vault: any, parsedDataLog: any): Record<string, string> {
  const data: Record<string, string> = {};
  
  // From profile basic fields
  if (profile.businessName) data.business_name = profile.businessName;
  if (profile.ownerName) data.owner_name = profile.ownerName;
  // ... all profile fields
  
  // From parsedDataLog (Gemini-extracted data) — this is the richest source
  if (parsedDataLog) {
    const parsed = typeof parsedDataLog === 'string' ? JSON.parse(parsedDataLog) : parsedDataLog;
    
    // Flatten all categories: operations, safety, menu_and_prep, license_info
    for (const [category, fields] of Object.entries(parsed)) {
      if (typeof fields === 'object' && fields !== null) {
        for (const [key, fieldData] of Object.entries(fields as any)) {
          if (fieldData?.value) {
            data[key] = fieldData.value;
            // Also store with category prefix for broader matching
            data[`${category}_${key}`] = fieldData.value;
          }
        }
      }
    }
  }
  
  // From vault (if exists, overwrites with curated data)
  if (vault) {
    if (vault.businessName) data.business_name = vault.businessName;
    if (vault.menuItems) data.menu_items = vault.menuItems;
    if (vault.foodSource) data.food_source = vault.foodSource;
    // ... all vault fields
  }
  
  return data;
}
```

The key insight: the parsedDataLog contains categories like `safety.hot_holding`, `safety.cold_storage`, `safety.waste_water`, `menu_and_prep.menu_items`, `menu_and_prep.food_source`, `operations.sanitizing_method`, etc. ALL of this data needs to be in the map.

**FIX PART 3 — Better semantic matching for questionnaire fields:**

PDF form fields often have long descriptive names like:

- “3. Describe how frozen, cold, and hot foods will be transported to the Temporary Food Establishment”
- “1. List all food and beverage items to be prepared and served”
- “Will All Foods Be Prepared At The TFE Site?”
- “Expected Number of Patrons Per Day”

Add these additional matching rules to connect PDF fields to profile data:

```typescript
// Food/menu related
if (fieldLower.includes('food and beverage') || fieldLower.includes('menu') || fieldLower.includes('prepared and served'))
  → match to data.menu_items

if (fieldLower.includes('transported') || fieldLower.includes('transport'))
  → match to data.food_transport or data.hot_holding + data.cold_storage

if (fieldLower.includes('prepared at') || fieldLower.includes('tfe site') || fieldLower.includes('food preparation'))
  → match to data.prep_location

if (fieldLower.includes('patrons per day') || fieldLower.includes('expected number'))
  → this is genuinely event-specific, SHOULD be asked

if (fieldLower.includes('water supply') || fieldLower.includes('potable water'))
  → match to data.water_supply

if (fieldLower.includes('hand washing') || fieldLower.includes('handwashing'))
  → match to data.sanitizing_method or data.handwash_station

if (fieldLower.includes('utensil washing') || fieldLower.includes('warewashing'))
  → match to data.sanitizing_method

if (fieldLower.includes('waste water') || fieldLower.includes('wastewater'))
  → match to data.waste_water

if (fieldLower.includes('food temperature') || fieldLower.includes('temp monitoring'))
  → match to data.temp_monitoring

if (fieldLower.includes('source') && fieldLower.includes('meat'))
  → match to data.food_source
```

**FIX PART 4 — Only ask TRULY missing questions:**

After matching, the questions list should be MUCH shorter. For the West Hartford Temporary Food Permit, the only questions that should remain are ones the profile genuinely can’t answer:

- “Expected Number of Patrons Per Day” (event-specific, not in profile)
- “Date and Time TFE will be set up and ready for inspection” (event-specific)
- Maybe “Name of Event” if not captured in the permit wizard

Questions about food items, transport, water supply, sanitizing, temperatures, etc. should ALL be auto-filled from the profile since we can see in the screenshots that the profile has this data at 95-100% confidence.

**FIX PART 5 — Show users what WAS auto-filled:**

In the questionnaire modal, before the questions, add a summary:
“✓ X fields will be auto-filled from your profile”
Then only show the truly missing fields below that.

This reassures users that their uploaded documents are being used.

**TEST:**

1. Go to West Hartford permit → click Temporary Food Permit Application → Generate
1. The questionnaire should now only ask 2-3 event-specific questions (patrons per day, event name, inspection date)
1. It should NOT ask about food items, transport, water supply, sanitizing, etc. — those are all in the profile

```

```